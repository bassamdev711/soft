# نتائج الفحص البصري الأولي — مشروع soft

## الصفحة الرئيسية

- الانطباع العام احترافي جزئيًا من ناحية اللغة العربية واتجاه RTL، مع هوية داكنة وحركة بسيطة.
- الرسالة الأساسية واضحة، لكن الصفحة تعتمد بدرجة كبيرة على نصوص عامة ومجردة ولا تعرض أدلة عملية على الخبرة أو النتائج.
- منطقة الصورة الرئيسية ظهرت بصريًا كمساحة داكنة/فارغة في المعاينة، مما يضعف الانطباع الأول ويحتاج إلى التحقق من تحميل الأصول في بيئة الإنتاج.
- شريط التنقل والواجهة يظهران أثناء التطوير مع مؤشرات Next.js؛ يجب التحقق من أن نسخة الإنتاج لا تعرض أدوات التطوير.
- توجد روابط إلى الأعمال والتواصل والمراجعات ضمنيًا، لكن لا تظهر مراجعات أو شعارات عملاء أو أرقام موثقة أو مؤشرات ثقة في الصفحة الرئيسية.
- أزرار الدعوة لاتخاذ إجراء موجودة، لكن لا يوجد مسار مميز يوضح زمن الرد أو طريقة التواصل أو ما يحدث بعد إرسال الطلب.
- هناك تفضيل قياس استخدام ظاهر في أسفل الصفحة، وهو جيد من ناحية الشفافية، لكنه يحتاج إلى مراجعة الصياغة والوضوح وربطه بسياسة خصوصية كاملة.

## صفحة الأعمال /work

- الصفحة لا تعرض أي أعمال أو دراسات حالة فعلية، بل تعرض عبارة "معرض قيد البناء".
- الصراحة بشأن عدم نشر مشاريع غير موثقة إيجابية، لكنها تجعل صفحة الأعمال لا تؤدي وظيفة الثقة التجارية المطلوبة لشركة تقنية.
- لا توجد كيانات أعمال في قاعدة البيانات، ولا صور/نتائج/مراحل/تقنيات/شهادات عميل مرتبطة بأعمال قابلة للإدارة.
- توجد مبادئ جيدة مثل السياق قبل الصورة والسرية والتعلم بعد الإطلاق، لكنها تحتاج إلى تحويلها إلى دراسات حالة حقيقية أو أمثلة مصرح بها.

## الخلاصة البصرية

الأساس التصميمي جيد ويمكن تطويره، لكن المشروع حاليًا يبدو كموقع تعريفي أولي أنيق أكثر من كونه منصة شركة تقنية مكتملة. الفجوة الأساسية ليست في الألوان وحدها؛ بل في غياب الأدلة، المحتوى المؤسسي، حالات الاستخدام، مسارات الثقة، والعمليات الإدارية الظاهرة.

## لوحة التحكم /admin

- المسار محمي بتسجيل دخول بريد/كلمة مرور ويعرض واجهة بسيطة جدًا.
- لا توجد في الحالة غير المسجلة أي مؤشرات على استعادة كلمة المرور أو المصادقة متعددة العوامل أو إدارة الجلسات.
- تصميم الدخول صغير ومتمركز ويشبه نموذجًا أوليًا أكثر من بوابة إدارة مؤسسية؛ يحتاج إلى هوية واضحة، شرح أمني مختصر، حالات خطأ، وحماية أفضل من محاولات الدخول.
- وجود مؤشرات أدوات التطوير في المعاينة يؤكد ضرورة فحص نسخة الإنتاج قبل الإطلاق.

## صفحة التواصل /contact

- الصفحة جيدة لغويًا وتشرح مسار التواصل، لكنها لا تحتوي فعليًا على بيانات اتصال موثوقة مثل بريد رسمي، هاتف، عنوان/منطقة خدمة، حسابات اجتماعية، ساعات عمل، أو زمن استجابة متوقع.
- الاعتماد على رابط نموذج الطلب فقط يجعل الشركة تبدو غير قابلة للوصول خارج المسار الواحد.
- عبارة التعامل المسؤول مع البيانات إيجابية، لكنها تحتاج إلى روابط فعلية لسياسة الخصوصية وشروط الاستخدام وموافقة واضحة على معالجة البيانات.
- لا توجد عناصر تحقق مؤسسية مثل اسم قانوني، رقم تسجيل، نطاق بريد رسمي، شهادات، شركاء، أو ملفات قابلة للتنزيل.

## ملاحظة تقنية أثناء الفحص

- `pnpm check` نجح، لكن `pnpm build` لم يكتمل لأن خطوة Drizzle تحتاج إلى `DATABASE_URL` أو `POSTGRES_URL` غير متوفر في بيئة الفحص. هذا ليس خطأ واجهة، لكنه يمنع التحقق من بناء الإنتاج ويجب حسمه في بيئة النشر.

## البنية والميزات الحالية

المشروع يملك صفحات تعريفية أساسية: الرئيسية، من نحن، الخدمات، تفاصيل الخدمة، الأعمال، الأسئلة الشائعة، التواصل، وطلب مشروع، إضافة إلى `/admin` و`/admin/content`. هذا يوفر أساس موقع شركة، لكنه لا يمثل بعد منصة تشغيل أو إدارة محتوى متكاملة.

في الخلفية توجد مسارات `projectRequests` و`siteContent` فقط. طلبات المشاريع يمكن استقبالها وعرضها للمشرف، لكن لا توجد عمليات تغيير حالة متقدمة، تعيين مسؤول، ملاحظات داخلية، متابعة، أرشفة قابلة للتدقيق، إشعارات، أو سجل نشاط. طبقة المحتوى تسمح بتحرير قيم عامة محدودة، لكنها ليست نظام إدارة أعمال أو أعمال ومراجعات.

لا توجد جداول أو مسارات للمشاريع/الأعمال، دراسات الحالة، المراجعات، العملاء، الشهادات، ملفات الصور، الرسائل، أو إعدادات الشركة. لذلك فإن إضافة هذه العناصر تحتاج تصميم نموذج بيانات وموافقات وصلاحيات، لا مجرد إضافة بطاقات في الواجهة.

## ضمان الجودة والبناء

لا توجد ملفات اختبارات تلقائية في المشروع، ولذلك فشل `pnpm test` برسالة عدم وجود ملفات اختبار. أمر `pnpm lint` غير صالح عمليًا في وضعه الحالي؛ إذ يستدعي `next lint` الذي بدأ إعداد ESLint تفاعليًا وانتهى بفشل، ويحتاج إلى نقل lint إلى ESLint CLI مع إعداد ثابت غير تفاعلي.

فحص TypeScript (`pnpm check`) نجح. أما البناء (`pnpm build`) فتوقف قبل بناء Next.js لأن `drizzle-kit push` يحتاج إلى `DATABASE_URL` أو `POSTGRES_URL`. يجب فصل مزامنة قاعدة البيانات عن build أو توفير اتصال قاعدة بيانات صحيح في بيئة النشر، مع تشغيل migrations كخطوة نشر مستقلة.

## الحكم الحالي

المشروع هو موقع تسويقي عربي RTL بقاعدة بيانات أولية ونموذج طلب ولوحة إدارة بسيطة. قوته في اللغة، اتجاه RTL، وضوح الخدمات، والأساس البصري المتماسك. ضعفه الحاسم هو غياب الإثبات التجاري، المحتوى القابل للإدارة، نظام المراجعات، إدارة الأعمال، اتصالات موثوقة، اختبارات، وبنية تشغيلية قابلة للتوسع.

## المعاينة بعد التنفيذ الجزئي

- ظهر شعار Aura الجديد في الرأس والتذييل عبر المسار المحلي `/brand/aura-logo-mark.png` بدل مسار التخزين الفارغ، وظهر كعلامة زرقاء/سماوية صغيرة متناسقة مع الهوية الداكنة.
- صفحة الأعمال أصبحت مرتبطة بمسار بيانات حقيقي: تعرض الأعمال المنشورة عند توفرها، وتحافظ على حالة فارغة صادقة واحترافية عند عدم وجود أعمال معتمدة.
- لا تزال صورة hero الرئيسية القديمة تظهر كمساحة داكنة شبه فارغة في المعاينة، وهي نقطة منفصلة تحتاج استبدال الأصل أو إصلاح مسار الأصل في مرحلة صقل الأصول العامة.
- مؤشرات Next.js DevTools الظاهرة في المعاينة مرتبطة بوضع التطوير؛ يجب التأكد من عدم ظهورها في نسخة الإنتاج.

## معاينة التواصل والإدارة بعد التنفيذ

- صفحة التواصل أصبحت تحتوي على نموذج رسائل مستقل، مع بيانات اتصال قابلة للتغذية من إعدادات الشركة؛ في بيئة بلا قاعدة بيانات يظهر fallback صريح بدل بيانات وهمية.
- تجربة النموذج تعرض حالات الإرسال والنجاح والفشل، وتوضح استخدام البيانات، مع رابط منفصل لطلب المشروع.
- بوابة الإدارة ما زالت تبدأ بشاشة دخول بسيطة؛ مسارات الإدارة الجديدة أُضيفت في الكود والتنقل، لكن اختبارها بعد الدخول يحتاج بيانات اعتماد وبيئة قاعدة بيانات فعلية.
- جميع معاينات التطوير تظهر مؤشرات Next.js DevTools، وهي ليست جزءًا من واجهة الإنتاج ويجب التحقق من غيابها عند النشر.

## الشعار الرسمي الذي اختاره المستخدم

- تم التحقق من الصورة الأصلية بأبعاد 8192×8192 ونسبة 1:1.
- تحتوي الصورة على رمز Aura الذهبي/الرمادي مع كتابة Aura وSoft على خلفية داكنة.
- تم تجهيز نسخة كاملة محسنة للويب بحجم 1600×1600 ونسخة رمز مقصوصة بحجم 640×640؛ النسخة المقصوصة تحافظ على الرمز دون الكتابة، والنسخة الكاملة تحافظ على الهوية النصية كما قدمها المستخدم.
- النسخة الكاملة مناسبة للمساحات البصرية الكبيرة أو صفحة الهوية، بينما النسخة المقصوصة مناسبة للرأس والتذييل والأيقونة.

## مواضع دمج الشعار الرسمي

- نسخة الرأس الحالية تستخدم `AuraMark`، وستُربط بنسخة الرمز المقصوصة من الشعار الرسمي بدل الرمز السابق.
- التذييل في أحدث تصميم GitHub يستدعي `AuraMark` فقط؛ سيتم استبداله داخل مساحة الهوية بنسخة lockup الكاملة التي تحتوي رمز Aura وAura وSoft.

## تحديث الخدمات والتواصل والترقيم

أظهرت معاينة صفحة الخدمات أن خدمة «تطبيقات الهواتف» تظهر الآن كبطاقة مستقلة وبالرابط `/services/mobile-apps`، كما تظهر في خيارات طلب المشروع. أظهرت المعاينة أيضًا أن الترقيم الظاهر أُزيل من عناوين الصفحات وبطاقات الخدمات وخطوات التواصل والطلب، وأصبحت العناوين نصية مباشرة.

تظهر صفحة التواصل الآن البريد الافتراضي `info@aurasoft.ye` والهاتف اليمني `+967 777 000 000` والموقع «صنعاء، الجمهورية اليمنية» مع روابط mailto وtel وتنبيه «بيانات تجريبية». تظهر البيانات نفسها في التذييل. يجب تغييرها من إعدادات الشركة قبل الإطلاق الفعلي.
