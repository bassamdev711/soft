# إعدادات الأمان المطلوبة للإنتاج

قبل نشر فرع التحصين على Vercel، يجب إضافة المتغيرات التالية في **Production** و**Preview** حسب الحاجة:

| المتغير | المطلوب |
|---|---|
| `JWT_SECRET` | قيمة عشوائية قوية بطول 32 محرفاً أو أكثر، ويفضل 64 محرفاً |
| `ADMIN_EMAIL` | بريد الإدارة الحقيقي |
| `ADMIN_PASSWORD` | كلمة مرور قوية بطول 12 محرفاً أو أكثر |
| `OWNER_OPEN_ID` | معرّف ثابت لحساب الإدارة |
| `UPSTASH_REDIS_REST_URL` | رابط Redis من Upstash |
| `UPSTASH_REDIS_REST_TOKEN` | رمز Redis من Upstash |
| `PUBLIC_APP_ORIGIN` | اتركه `https://orasoft.vercel.app` أو ضع النطاق الرسمي |

الموقع أصبح يرفض جلسات الإدارة وطلبات API الحساسة عند غياب إعدادات الإنتاج الأمنية بدلاً من استخدام أسرار fallback. كما أن المعدلات موزعة عبر Upstash حتى تعمل الحماية عبر جميع دوال Vercel، وليست عداداً محلياً في ذاكرة دالة واحدة.

لا تضع هذه القيم في المستودع أو في ملفات `.env` المرفوعة إلى GitHub. بعد إضافتها، نفّذ اختباراً واحداً لتسجيل الدخول الصحيح، ثم اختبر أن خمس محاولات فاشلة لا تتجاوز الحد وأن المحاولة التالية تعيد `429`، مع عدم استخدام كلمة المرور الحقيقية في سجلات أو لقطات الاختبار.
